pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo "Build in progress"
            }
        }

        stage('Test') {
            steps {
                echo "Test in progress"
            }
        }
    }
}
